# TIDEBASE — 作業の約束

静岡県西部（浜名湖・遠州灘）で使う釣行記録アプリ。
使う人は本人と、招待した友人だけ。Supabase 無料枠 + GitHub Pages で運用する。

## 毎回やること

**画面の見た目や動きを変えたら、`frontend/assets/news.json` の先頭に 1 件足す。**
これは頼まれてからやることではなく、変更を配るたびに必ずやる（D-079）。

```json
{
  "at": "2026-08-09 07:04",
  "title": "1 行で「何が変わったか」が分かるように書く",
  "items": [{ "kind": "new", "text": "使う人の言葉で、画面名と操作で説明する" }]
}
```

- `at` は **JST の `YYYY-MM-DD HH:MM`**（24 時間表記）。
  文字列のまま比べて新旧を決めるので桁を崩さない。未読の判定にも使う。
- **このコンテナは UTC で動いている。** `date` や `git log` の時刻をそのまま写すと
  9 時間ずれる（D-083 で実際にやった）。必ずこれで JST に直してから書く:

  ```sh
  TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M'
  ```
- **曜日は書かない。** 日付から計算して画面で出している。
- **同じ機能を 1 時間以内に直したら、新しく足さずに前の 1 件へまとめる**（D-094）。
  前の `at` を配り直した時刻に更新し、`title` と `items` を書き直す。
  - **直した経過ではなく、いまどうなっているかを書く。** そうしないと
    「A にした」「やっぱり B にした」が並び、読む人には何が正か分からない。
    まとめた結果、途中の言い直しは消えてよい。
  - 「同じ機能」は**使う人から見た同じ画面・同じ機能**。中の実装が違っても、
    使う人にとって「アイコンの話」なら 1 件。
  - 1 時間は**直前に配った時刻から**数える。20:19 → 20:33 → 21:05 のように
    続けて直したときは、間隔がそれぞれ 1 時間以内なので全部で 1 件になる。
  - 別の機能なら、何分後でも分けて足す。時間ではなく中身で決める。
- 上のまとめに当てはまらなければ、**配った回数だけ足す**。畳んであるので長くならない。
- `kind` は `new`（新しくできるようになったこと）か `fix`（直したこと）。
- `title` は畳んだ状態で見える唯一の行。ここだけで伝わるように書く。
- **中身は使う人の言葉で書く。** 内部の用語・仕組み・判断の理由は書かない。
  それは `docs/DECISIONS.md` に書く。読み手が違う。

DB やテストだけの変更（画面から見えないもの）は足さなくてよい。

## 判断を残す

`docs/DECISIONS.md` に D-番号で追記する。**何を決めたかより、なぜそう決めたか**と、
**確かめた方法**を書く。前の判断を取り消すときは、取り消したことを明記する。

## 検証データ

本番の Supabase を直接使っている。**テスト用アカウントは
`%@tidebase.test` のメールだけ**を使い、検証が終わったらそのパターンで消す。
実ユーザーのデータが同じテーブルにあるので、条件なしの DELETE は絶対にしない。

## 気をつけていること

- **TDZ**: `const` / `let` は巻き上がらない。描画関数より後に宣言すると、
  最初の描画で落ちる。この取り違えを何度もやっている（D-077 では 1 画面で 2 回）。
- **RLS**: ポリシーの中から別の保護テーブルを参照しない。`SECURITY DEFINER` の
  補助関数を挟む。`security_invoker = false` のビューは WHERE 句が境界そのもの。
- **公開リポジトリ**: 秘密は 1 つも置かない。anon（publishable）キーは公開前提だが、
  それだけで通る API は実質誰でも叩けると考える。
