# 🤝 TIDEBASE 委任運用ガイド v1.0

> **2026-07-17 | 目的: ユーザーの構築・運用作業を「一度きりのセットアップ + 承認」に最小化し、
> 以降の実作業を Claude Code に委任する**

## 1. 役割分担（このガイド適用後）

> ⚠️ **v1.0 からの変更（D-021）**: AWS 撤去により AWS キーの登録は**不要になった**。
> `*.amazonaws.com` の許可も不要。本ガイドの AWS 関連記述は読み替え済み。

| 作業 | 担当 |
|------|------|
| コーディング・テスト・設計・ドキュメント | Claude Code |
| DB マイグレーション適用・スキーマ確認 | Claude Code（`scripts/supabase_admin.py`） |
| 潮汐観測点マスタ生成・気象庁疎通確認 | Claude Code（ネットワーク許可後） |
| Edge Function デプロイ・Supabase Auth 設定 | Claude Code（Management API 経由） |
| フロントのデプロイ | GitHub Actions（自動） |
| **PR の確認・マージ** | **あなた**（GitHub 上でワンクリック） |
| **シークレットの発行・登録・ローテーション** | **あなた**（下記の一度きり作業） |
| **課金・アカウント操作、実機での動作確認** | **あなた** |

## 2. 一度きりのセットアップ（合計 15 分程度）

Claude Code on the Web の**環境設定**（Environment settings）で以下を行う。
設定方法の詳細: https://code.claude.com/docs/en/claude-code-on-the-web

### 2.1 ネットワークポリシーにドメインを許可（5 分）

現在のサンドボックスは外部サイトがほぼ遮断されており、これが委任の最大のブロッカー。
環境のネットワークポリシーで以下を許可する（カスタム許可リスト方式の場合）:

| ドメイン | 用途 |
|---------|------|
| `www.data.jma.go.jp` | 潮汐データ取得・観測点マスタ生成 |
| `api.supabase.com` | Management API（マイグレーション適用・Edge Function デプロイ・Auth 設定） |
| `*.supabase.co` | Supabase REST / Auth / Edge Functions（結合テスト・疎通確認） |
| `api.open-meteo.com` / `marine-api.open-meteo.com` / `archive-api.open-meteo.com` | 天気・波高（Phase 2） |

### 2.2 シークレットを環境変数に登録（10 分）

環境設定の環境変数（Secrets）に以下を登録する。**リポジトリには絶対にコミットしない**。

**① Claude Code 環境のシークレット**

| 変数名 | 取得場所 |
|--------|---------|
| `SUPABASE_ACCESS_TOKEN` | supabase.com → アカウントアイコン → Access Tokens → Generate new token |
| `SUPABASE_PROJECT_REF` | ダッシュボード URL `supabase.com/dashboard/project/{この部分}` |
| `SUPABASE_URL` | Project Settings → API（`https://xxxx.supabase.co`） |
| `SUPABASE_ANON_KEY` | Project Settings → API Keys（公開前提の値） |

**② GitHub リポジトリの Secrets**（Settings → Secrets and variables → Actions、keepalive 用）

| 変数名 | 値 |
|--------|-----|
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` | ①と同じ値 |

### 2.3 リポジトリ public 化 + GitHub Pages 準備（D-022・5 分）

| # | 操作 |
|---|------|
| 1 | リポジトリ → Settings → General → Danger Zone → **Change visibility → Make public** |
| 2 | Settings → Advanced Security（Code security）→ **Secret scanning** と **Push protection** を有効化（誤コミットの入口遮断） |
| 3 | Settings → Pages → Source を **GitHub Actions** に設定（フロント実装 PR が来た時点でも可） |

### 2.4 セキュリティ上の注意

- `SUPABASE_ACCESS_TOKEN` は**アカウント全体に効く**強い権限。個人開発の
  単一プロジェクトなら実害は限定的だが、不要になったら削除・定期ローテーション推奨
- AWS キーは **D-021 により不要**（渡さないこと自体が最善の対策）
- 環境変数はセッションのサンドボックス内でのみ使われ、コミット対象にならない
  （`.gitignore` 済みの `.env` にも書かない運用とする）

## 3. セットアップ後に Claude Code へ頼めること（例）

```
「マイグレーションを適用して」
  → scripts/supabase_admin.py apply（適用済み管理付き・冪等）
「スキーマの実態を確認して v1.3 と突合して」
  → scripts/supabase_admin.py inspect → 設計補完書 8 章の台帳と突合 → 差分修正
「観測点マスタを生成して」
  → scripts/generate_tide_stations.py → JSON 差し替え → テスト → コミット
「潮汐 API の実疎通を確認して」
  → 気象庁から実データ取得 → パーサー検証 → 問題あれば修正
「Edge Function をデプロイして」
  → scripts/deploy_function.py <slug>（Management API 経由）→ 疎通確認
「友人を招待して」
  → 設定 → GROUP で招待リンクを発行（1 回きり・7 日間有効）。
     LINE などで本人にだけ送る。一般公開のサインアップは無効のまま（D-041）。
     招待を作れるのは app_admins に載っている管理者だけ（D-043）
「管理者を増やして／減らして」
  → 6 章の手順を実行する（アプリからは変更できない設計）
「Supabase の Auth 設定をして」
  → Management API で Email/Password 有効化・Site URL 設定
```

## 4. 進め方の推奨

1. 上記セットアップ完了を Claude Code に伝える
2. Claude Code が「スキーマ突合 → マイグレーション適用 → 観測点マスタ生成 →
   JMA 疎通確認」を一括実行（設計補完書 11 章の順序）
3. 以降は機能単位で PR を作成 → あなたはレビュー・マージのみ

---

*TIDEBASE 委任運用ガイド v1.0 — 2026-07-17*

---

## 5. 独自ドメインに変える場合の手順（検討用メモ・2026-08-07）

現在: `https://siorine21.github.io/tidebase/`（GitHub Pages のプロジェクトページ）

> ⚠️ ドメイン取得は**唯一の継続課金**になる（D-010 / D-021 の「課金経路を持たない」構成から外れる）。
> 既に持っているドメインのサブドメインを使うなら追加費用は無い。

### あなたの作業

| # | 作業 | 場所 |
|---|------|------|
| 1 | ドメインを取得（サブドメイン利用なら不要） | レジストラ |
| 2 | DNS レコードを追加（下表） | DNS |
| 3 | Settings → Pages → Custom domain に入力 → 保存 | GitHub |
| 4 | 証明書が発行されたら **Enforce HTTPS** にチェック | GitHub |

**DNS（サブドメイン推奨・CNAME 1 本で済む）**

```
app.example.com.   CNAME   siorine21.github.io.
```

**apex（example.com そのもの）を使う場合は A レコード 4 本**

```
example.com.  A  185.199.108.153
example.com.  A  185.199.109.153
example.com.  A  185.199.110.153
example.com.  A  185.199.111.153
```

CAA レコードを設定している場合は `letsencrypt.org` を許可しておく
（GitHub の証明書発行が通らなくなる）。

### Claude Code 側の作業

| # | 作業 | 備考 |
|---|------|------|
| 1 | `frontend/CNAME` を追加（内容はドメイン 1 行） | Actions デプロイでも設定が消えない保険 |
| 2 | Supabase Auth の `site_url` / `uri_allow_list` を更新 | Management API で実行可能 |
| 3 | 疎通確認（ログイン・招待リンク・潮汐 API） | |

### 変えなくてよいもの

- **アプリ内のリンク**: すべて相対パス。`/tidebase/` → `/` への変化に影響を受けない（確認済み）
- **招待リンク**: `inviteUrl()` が `location.href` から組み立てるため自動で新ドメインになる
- **Supabase の API ドメイン**: `*.supabase.co` のままでよい
  （Supabase の独自ドメインは月 10 ドルの有料アドオン。不要）
- **発行済みの招待リンク**: 旧 URL は新ドメインへリダイレクトされるため生き続ける

### 注意

- HTTPS の証明書が出るまで（通常 15 分・最大 24 時間）は HTTP になる。
  この間は `navigator.clipboard` が使えず、招待リンクのコピーが
  「選択して手動コピー」の経路に落ちる（実害はない）。

---

## 6. 管理者を追加・削除する手順（D-043・2026-08-07 に実機で検証）

管理者かどうかは `public.app_admins` テーブルだけで決まる。この表は
**RLS 有効・ポリシー無し**なので、アプリからは誰も読み書きできない。
変更は Claude Code が Management API 経由で行う（＝あなたが依頼する）。

### まず決めること: どちらの「管理者」か

`app_admins` に載せるだけでは、**あなたのグループには招待できない**。
招待の条件は「管理者 **かつ** そのグループのオーナー」の 2 つだから。
実機で確認した挙動は次のとおり。

| app_admins | あなたのグループでの role | できること |
|---|---|---|
| × | member | 何もできない（今の友人 3 人はここ） |
| ○ | member | **自分の別グループを作って、そこへ招待できる**。あなたのグループには招待できない |
| ○ | owner | あなたのグループに招待できる（＝共同管理者） |
| × | owner | 招待できない（管理者フラグが外れた時点で即座に不可） |

つまり:

- **共同管理者にしたい**（自分のグループに一緒に招待してほしい）
  → `app_admins` に追加 **＋** `group_members.role` を `owner` に変更（下記 A）
- **別グループを任せたい**（その人の輪は分ける）
  → `app_admins` に追加するだけ（下記 B）

### A. 共同管理者にする

```sql
-- 1. 対象の user_id を調べる
SELECT u.id, u.email, p.username
  FROM auth.users u JOIN public.profiles p ON p.id = u.id
 WHERE u.email = 'someone@example.com';

-- 2. 管理者に追加
INSERT INTO public.app_admins (user_id, note)
VALUES ('<uuid>', '共同管理者 2026-08-07');

-- 3. あなたのグループのオーナーに昇格
UPDATE public.group_members SET role = 'owner'
 WHERE group_id = '<group_uuid>' AND user_id = '<uuid>';
```

### B. 別グループを任せる

```sql
INSERT INTO public.app_admins (user_id, note)
VALUES ('<uuid>', '別グループ管理 2026-08-07');
```

その人が自分で設定 → GROUP からグループを作り、招待できるようになる。

### C. 管理者から外す

```sql
DELETE FROM public.app_admins WHERE user_id = '<uuid>';
-- 共同管理者だった場合は role も戻す
UPDATE public.group_members SET role = 'member'
 WHERE group_id = '<group_uuid>' AND user_id = '<uuid>';
```

**外した瞬間に招待は作れなくなる**（オーナーのままでも不可）。
ただし**発行済みの招待リンクは生きたまま**なので、必要なら消す:

```sql
DELETE FROM public.group_invites
 WHERE created_by = '<uuid>' AND used_at IS NULL;
```

### 現状

管理者は 1 名（`uyhki21@gmail.com`・012 の移行で自動登録）。
一覧はこれで確認できる:

```sql
SELECT u.email, p.username, a.note, a.granted_at
  FROM public.app_admins a
  JOIN auth.users u ON u.id = a.user_id
  JOIN public.profiles p ON p.id = a.user_id
 ORDER BY a.granted_at;
```
