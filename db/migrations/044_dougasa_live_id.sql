-- ============================================================
-- 同笠海岸のライブカメラの動画 ID を差し替える
--
-- 043 で入れた kQljrmctUkg は**アーカイブ（配信が終わった録画）**だった。
-- 本人が「おそらくアーカイブされたライブ映像のリンクを送ってしまった」と気づいて
-- 生配信のほうを教えてくれた。録画のままだと、いつ見ても同じ過去の海が映る。
-- 「まずは海の様子をチェックする」ためのものなので、これでは用をなさない。
--
-- **YouTube のライブは、配信を切り直すと動画 ID が変わる。**
-- 24 時間つなぎっぱなしなら変わらないが、日ごとに配信し直す運用だと
-- また同じことが起きる。そのときは黙って古い録画が映るので気づきにくい。
-- 直す道は D-143 に書いた（チャンネル ID を持って live_stream?channel= で埋める）。
-- ============================================================

BEGIN;

UPDATE public.live_cameras
SET youtube_id = '84GhMEo9We0'
WHERE code = 'ENSHU-DOUGASA';

COMMIT;
